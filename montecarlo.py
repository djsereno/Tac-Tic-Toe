import random
import time

def monteCarlo(board, numSimulations):
    """Return the next best move based on a Monte Carlo simulation."""

    # For now, randomly make a move
    return board.getRandomMove()